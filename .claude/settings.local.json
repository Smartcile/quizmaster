{
  "permissions": {
    "allow": [
      "Bash(git add *)",
      "Bash(git commit -m 'fix: Use dynamic hostname for API calls instead of hardcoded localhost *)",
      "Bash(git push *)",
      "Bash(docker-compose config *)",
      "Bash(docker-compose -f docker-compose.prod.yml config --quiet)",
      "Bash(git commit -m 'docs: Bring README, .env.example, and example.csv up to date *)",
      "Bash(git commit -m 'ci: Bump build-push-action to v6, add setup-qemu *)"
    ]
  }
}
