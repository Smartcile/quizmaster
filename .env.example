# Database credentials (used by both postgres and backend)
DB_USER=quiz_user
DB_PASSWORD=quiz_password
DB_NAME=quiz_master

# Optional: Node environment
NODE_ENV=production

# Optional: Override host ports (defaults: 3001, 3002, 3003)
# ADMIN_PORT=3001
# SLIDESHOW_PORT=3002
# QUIZZER_PORT=3003
