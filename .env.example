# Database Configuration
DB_USER=quiz_user
DB_PASSWORD=quiz_password
DB_NAME=quiz_master
DB_HOST=postgres
DB_PORT=5432

# Node Environment
NODE_ENV=production

# Server
PORT=5000

# Frontend API URLs (adjust for your domain)
VITE_API_URL=http://localhost:5000/api

# Optional: CORS settings
CORS_ORIGIN=*
